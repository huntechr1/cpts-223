
#include <iostream>
#include "App.h"

using std::cout;
using std::cin;
using std::endl;

int main(void)
{
App a;
a.runApp();
/*char name[100] = "\0";
a.getFileName(name);
a.openFile(name);
    if(a.isOpen())
    {
        cout << "file open" << endl;
    }
    else
    {
        cout << "failed to open" << endl;
    }
a.readFile();
a.getList()->printList();*/


return 0; 
}
